# ─────────────────────────────────────────────────────────────
# neural-ode-galaxy-gen — top-level dependencies only.
# Transitive packages (CUDA/nvidia-*, triton, jupyter sub-deps,
# umap's numba/llvmlite/pynndescent, TF's absl/ml_dtypes/…) are
# resolved automatically and intentionally left out for portability.
#
# torch pulls the matching CUDA runtime wheels itself on Linux/Windows.
# For a specific CUDA build, install torch from the right index, e.g.:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu130
# ─────────────────────────────────────────────────────────────

# ── Core deep learning ───────────────────────────────
torch==2.11.0
torchvision==0.26.0
torchdiffeq==0.2.5
torchmetrics==1.9.0
torch_fidelity==0.4.0

# ── Model inspection / visualization ─────────────────
torchinfo==1.8.0
torchviz==0.0.3
graphviz==0.21            # needs the system Graphviz binary (dot) installed separately

# ── Scientific stack ─────────────────────────────────
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
matplotlib==3.10.8

# ── Data / dimensionality reduction ──────────────────
h5py==3.16.0              # read Galaxy10 DECaLS .h5 directly (no astroNN/TF needed)
umap-learn==0.5.12        # pulls numba, pynndescent, llvmlite automatically

# ── Utilities ────────────────────────────────────────
tqdm==4.67.3
pillow==12.2.0

# ── Notebook runtime ─────────────────────────────────
ipykernel==7.2.0

# ── Exam CNN morphology classifier (Stage 3 evaluation, Keras/TF) ──
# TensorFlow has no wheels for Python 3.13 yet; skipped there,
# installed on <=3.12 environments (e.g. Kaggle, Colab).
tensorflow>=2.16; python_version < "3.13"
keras==3.15.0; python_version < "3.13"